apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ pv_name }}
spec:
  capacity:
    storage: 2Gi
  accessModes:
  - ReadWriteOnce
  nfs:
    path: {{ nfs_root | default('/nfsshare') }}/{{ pv_name }}
    server: {{ ansible_ssh_host }}
  persistentVolumeReclaimPolicy: Recycle